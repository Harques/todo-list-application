server:
    port: 9502

spring:
    application:
        name: todo-service
jpa:
    show-sql: true
    hibernate:
        ddl-auto: create

# disable eureka by default

eureka:
    client:
        enabled: false
